/** Estado de sessão em memória. Dados persistentes continuam no IndexedDB. */
export const state = {
  activeContestId: null,
  currentQuestionSession: null,
  currentSimulation: null,
  installPrompt: null,
};
